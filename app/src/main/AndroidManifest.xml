<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.adiaz.legasports">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".CategoriesActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.adiaz.legasports.CategoriesActivity"/>
        </activity>
        <activity
            android:name=".ChampionshipActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".CategoriesActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.adiaz.legasports.CategoriesActivity"/>
        </activity>
        <activity android:name=".FavoritesActivity">
        </activity>
        <activity android:name=".FavoriteTeamActivity">
        </activity>
        <activity android:name=".FootballActivity">
        </activity>
    </application>

</manifest>